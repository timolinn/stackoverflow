sudo: false
language: node_js
node_js:
    - "stable"
install:
    - npm install
services:
    - redis-server
    - mongodb
cache:
    directories:
        - node_modules
script:
    - npm run build
    - npm run test
